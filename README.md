# dig-nat
DIG NAT traversal — abstract peer connection over UPnP/IGD, PCP (RFC 6887), NAT-PMP, direct (port-forwarded), with relay.dig.net as last-resort fallback. One connect() API; the traversal method is transparent to callers.
